trigger taskTrigger on Task (after insert, after update, after delete) 
{
	if(trigger.isAfter)
    {
        if(trigger.isInsert || trigger.isUpdate || trigger.isDelete)
        {
            ActivityHelperClass.updateActivityCountOnAccount(trigger.new, trigger.oldMap);
        }
    }
}